# ai-content-summarizer
This project is a Node.js and Express backend application that implements an AI Content Summarizer use case. It exposes a REST API that accepts text input and returns a summarized version using simulated AI logic. The project demonstrates clean backend structure, API design, and basic AI-driven processing.
